// Tailwind v4 uses @tailwindcss/postcss as the PostCSS plugin.
// No tailwind.config.js needed — all theme customisation is in globals.css.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
